# Copy to .env and fill in. Only VITE_* keys reach the browser bundle, so never
# put a client secret in here — the Google web client ID is public by design.

# Google Sign-In web client ID, from Google Cloud Console > Credentials.
# Format: <numbers>-<hash>.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=
